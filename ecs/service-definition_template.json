{
    "cluster": "${CLUSTER_NAME}",
    "serviceName": "${SERVICE_NAME}",
    "taskDefinition": "${TASK_DEFINITION}",
    "desiredCount": 1,
    "networkConfiguration": {
      "awsvpcConfiguration": {
        "subnets": ["${SUB_NET_1}", "${SUB_NET_2}", "${SUB_NET_3}"],
        "securityGroups": ["${SECURITY_GROUPS}"]
      }
    },
    "loadBalancers": [
      {
        "targetGroupArn": "arn:aws:elasticloadbalancing:us-east-1:727078712610:targetgroup/ecs-target-group/0f86f908e3dddce6",
        "containerName": "fiat-tech-producao",
        "containerPort": 3000
      }
    ]
  }
  
